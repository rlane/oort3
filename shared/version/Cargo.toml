[package]
name = "oort_version"
version = "0.77.0"
publish = false
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["cdylib", "rlib"]

[build-dependencies]
built = { version = "0.7", features = ["git2", "chrono", "semver"] }
