[package]
name = "oort_version"
version = "0.84.1"
publish = false
edition = "2024"
build = "build.rs"

[lib]
crate-type = ["cdylib", "rlib"]

[build-dependencies]
built = { version = "0.7", features = ["git2", "chrono", "semver"] }
