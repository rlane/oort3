[package]
name = "oort_proto"
version = "0.83.1"
publish = false
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4.31", features = ["serde"] }
