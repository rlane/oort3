[package]
name = "oort_proto"
version = "0.86.0"
publish = false
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4.31", features = ["serde"] }
